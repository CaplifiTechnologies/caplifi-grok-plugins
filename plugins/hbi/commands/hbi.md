# /hbi

Half-Baked Idea package helper.

1. Confirm package path or create `{slug}-YYYY-MM/` with full file set.  
2. Keep disclosure **closed** unless the user explicitly approves ship.  
3. Validate with `python3 tools/hbi_validate.py <path>`.

Spec: https://eclecticventures.net/hbi/
